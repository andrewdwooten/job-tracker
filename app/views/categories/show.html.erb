<h1>Category: <%= @category.title %></h1>

<h4><%= @category.title %> Jobs</h4>

  <ul>
    <% @category.jobs.each do |job| %>
      <li>
        <%= link_to job.title, company_job_path(job.company, job) %>
      </li>
    <% end %>
  </ul>
